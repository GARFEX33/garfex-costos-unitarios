# Implementation Tasks: Expose Public WRITE Deactivate/Reactivate on the Resource Master Core

This is the strictly ordered auto-chain for the Deactivate/Reactivate graduation of public WRITE. It builds directly on Create-WRITE (shipped, archived `2026-08-21-resource-master-core-write`) and Update-WRITE (shipped, archived `2026-08-20-resource-master-core-write-update`), and on design.md's fully worked "Task-unit breakdown (auto-chain)" table (U1/U2/U3/U4). Per-operation graduation is inherited and binding from the archived spec's "Separate read and per-operation write readiness" requirement and is not reopened here. `Deactivate` and `Reactivate` are graduated together because they share one internal mechanism (`setActiveRevision` catalog-side, symmetric `*Revision` methods resource-side); `HardDelete` remains designed on paper only — no unit in this plan compiles, exports, or stubs it.

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | ~650–950 total per design.md's Task-unit breakdown (4 methods, one structural novelty — the catalog confirm-read — plus three full reachability tables: catalog shared 6, resource Deactivate 5, resource Reactivate 7) |
| 400-line budget risk | Medium-High (per proposal.md's Risks table and design.md's Review-guard forecast) |
| Chained PRs recommended | Yes |
| Suggested split | U1 → U2 → U3 → U4 |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: Medium-High

Each unit is one stacked-to-main review boundary with its tests, focused receipt, rollback boundary, and full-suite proof. No unit may exceed its forecast in the table below. If measured work exceeds its forecast, split that unit along design.md's own file/table granularity; do not create a broad exception and do not improvise a new boundary.

### Per-slice forecast

| Slice | Forecast |
| ------- | ---------- |
| U1 | ≤250 changed lines |
| U2 | ≤300 changed lines |
| U3 | ≤300 changed lines |
| U4 | ≤100 changed lines |

Every unchecked implementation task below maps one-to-one to this forecast table.

## Global execution and compatibility contract

- Use strict RED → GREEN → TRIANGULATE → REFACTOR evidence for every unit that touches a package with tests. RED must be captured before the production behavior is implemented.
- For every slice, run the unit's exact focused `-run` command first, then the mandatory boundary command `go test ./... -count=1`; the latter is both the compile-safety proof for every current adapter, service, composition root, TUI call site, test double, and external test, and the full-suite proof. Record the exact command and result in the unit receipt.
- Per the `GARFEX_STRICT` focused-test discoverability gate (`.agents/skills/golang-hexagonal/references/garfex-strict-profile.md`): run the exact stated `-run` command, confirm it selects at least one test in every package the unit touched (`ok ... [no tests to run]` for a touched package is a failed gate even though `go test` exits 0), and name every new test function so at least one literal alternative from that unit's `-run` pattern appears as a substring of the function name after `Test`.
- Per the field-completeness gate: diff every public lifecycle-request field (`Kind`+`ID` or `ID`, `ExpectedRevision`, `Actor`) against its internal destination field-by-field at the bridge; an omission needs either a mapping or a one-line rationale comment at the omission site, never silence.
- Never widen `catalogWriter`/`resourceWriter` without adapting every in-repo fake in the same slice.
- Never change `core.Record`'s, `core.WithActor`'s, or `DiagnosticSink`'s existing signature. `Actor` travels only via `core.WithActor(ctx, ...)` / `core.ActorFrom(ctx)`, reusing Create/Update's mechanism as-is; never a domain or application function parameter; never persisted; never given a new column or migration; never exposed on a public DTO.
- `ExpectedRevision == 0` and `ID == 0` are both `INVALID_ARGUMENT` at the `Writer` shape gate for either request — CAS is mandatory; there is no "unconditional lifecycle toggle" request shape.
- `internal/app/*`, `internal/domain/*`, and `internal/postgres/*` stay at zero changed lines for the entire chain — unlike Update, no internal fix is needed: `internal/core/errors.go`'s `Map` already carries correctly-ordered `IdentityConflict`/`ReactivationImpossible` arms and is never edited.
- No unit compiles, exports, or stubs `HardDelete` as a symbol, an "unimplemented" branch, an unused request field, or any other runtime-discoverable artifact. `CatalogWriteRequest`/`ResourceWriteRequest`/`CatalogUpdateRequest`/`ResourceUpdateRequest` (Create, Update) stay untouched; Deactivate/Reactivate is purely additive.
- `resourcecore/copy.go` gets **no new `Clone*` function**: `CatalogLifecycleRequest`/`ResourceLifecycleRequest` are scalar-only (no map/slice field), so Go value semantics already give the caller-mutation-after-call guarantee. This is a documented design decision, not an oversight — see U1's non-task below. The returned `CatalogRecord`/`Resource` still go through the existing `CloneCatalogRecord`/`CloneResource` unmodified.
- The catalog side (both ops) issues a confirm-read (`a.catalog.Get`) after a successful `DeactivateRevision`/`ReactivateRevision` call, because that internal call returns only `error`. The resource side (both ops) issues **no** confirm-read — `domain.LifecycleResult.Resource` is already complete. This asymmetry between the two bridge halves is structural, not accidental, and each half's shape is proven by its own unit (U2 confirm-read presence and failure-mode test; U3 confirm-read absence via call-count assertion).
- The idempotent no-op / CAS-bypass asymmetry (catalog both ops + resource Reactivate short-circuit before the CAS compare and silently succeed on a stale `ExpectedRevision`; resource Deactivate enforces CAS strictly even on the no-op) is pre-existing, production-hardened `internal/app` behavior. It is reused as-is and documented, never fixed, per proposal.md's explicit non-goal.
- The READ contract and the compiled Create+Update WRITE surface stay byte-compatible: no read DTO, method, or error identity changes in any unit; no Create/Update method, field, or error identity changes in any unit.
- Inspect `git diff --name-only` and `git diff --stat` before and after every unit. Preserve unrelated user changes and preserve every other in-flight `openspec/changes/*` directory without edits.
- Do not run local `go build`, `docker build`, or blanket database/volume cleanup. U1–U3 use in-repo fakes for `catalogWriter`/`resourceWriter`, not a live database; live-PostgreSQL integration evidence for the asymmetry proof pair is a disclosed gap in a sandboxed apply session, matching Update's precedent — documented, not silently skipped.
- When work cannot fit in a unit's forecast, split into another additive unit along design.md's own file/table granularity rather than weakening these rules.

## Ordered implementation units

### U1 — Public lifecycle contract

- [x] Add `CatalogLifecycleRequest`, `ResourceLifecycleRequest` to `resourcecore/write_types.go` (additive; all prior request types untouched); add `Writer.DeactivateCatalog`/`Writer.ReactivateCatalog`/`Writer.DeactivateResource`/`Writer.ReactivateResource`, `WriteCapabilities` +4 methods (reaching 8 total), `validateCatalogLifecycleRequest`/`validateResourceLifecycleRequest` to `resourcecore/writer.go`; extend `resourcecore/writer_test.go` and `resourcecore/external_test.go`. No bridge change in this unit — `WriteCapabilities` grows to 8 methods with nothing implementing the new 4 in production yet. Satisfies spec requirements "Consumer-neutral public Deactivate/Reactivate for catalog and resource" and half of "Compiled surface extended to Create, Update, Deactivate, and Reactivate" (the compile-time half; the zero-touch half is U4's job). <!-- sdd-owner: implementation -->

  **Documented non-task — no `Clone*` function added.** `resourcecore/copy.go` is not touched by this unit or any other. `CatalogLifecycleRequest`/`ResourceLifecycleRequest` have zero map/slice fields; Go value semantics already give full immutability, confirmed against every existing `Clone*` in `copy.go`, each of which exists specifically to deep-copy a map or slice field neither new request has. An empty pass-through `Clone*` would be dead code. This is a deliberate design decision recorded in design.md's "Alternatives rejected" table, not an omission to fix later.

**Allowed edit surfaces:** exactly `resourcecore/write_types.go`, `resourcecore/writer.go`, `resourcecore/writer_test.go`, and `resourcecore/external_test.go`. `resourcecore/copy.go` is explicitly out of scope for this unit (see non-task above).

**Dependencies and end state:** start from Update-WRITE shipped and archived; `Reader`/`ReadCapabilities` and every Create/Update request/method remain untouched byte-for-byte. `Writer.DeactivateCatalog`/`ReactivateCatalog`/`DeactivateResource`/`ReactivateResource` validate request shape only: blank `Actor` → `INVALID_ARGUMENT`; `ID == 0` → `INVALID_ARGUMENT` (mirrors `setActiveRevision`'s `id == 0 || expectedRevision == 0` guard, `service.go:557-558`, and `recursos.Service`'s equivalent guards, `service.go:234,256`); `ExpectedRevision == 0` → `INVALID_ARGUMENT`. No unknown-`Kind` check and no `Values`/`Rules` shape loop — lifecycle requests carry no content payload; an invalid `Kind` surfaces as `NOT_FOUND` from the internal `Get` inside `setActiveRevision`, not a boundary shape rejection, mirroring Update's precedent for reference-existence checks. `WriteCapabilities` compiles exactly 8 methods (`CreateCatalog`, `CreateResource`, `UpdateCatalog`, `UpdateResource`, `DeactivateCatalog`, `ReactivateCatalog`, `DeactivateResource`, `ReactivateResource`); no `HardDelete` symbol, stub, or discoverable artifact exists. Requests pass by value (no `Clone*` needed, see non-task); returned `CatalogRecord`/`Resource` still go through the existing `CloneCatalogRecord`/`CloneResource`. Nothing implements `WriteCapabilities` in production yet with the widened 8-method shape — no bridge or composition wiring is affected by this unit; `internal/bridge/resourcecore.Adapter` is untouched until U2. Rollback removes only these four `resourcecore` files' lifecycle additions.

**Focused checks:** `go test ./resourcecore -run 'TestWriter|TestExternalWrite|TestLifecycleRequest' -count=1`.

**Strict evidence:** RED adds, before implementation:
  - `TestWriter_DeactivateCatalog_ShapeValidation` and `TestWriter_ReactivateCatalog_ShapeValidation` (`ID == 0`, `ExpectedRevision == 0`, blank `Actor`, each → `INVALID_ARGUMENT`, no capability call).
  - `TestWriter_DeactivateResource_ShapeValidation` and `TestWriter_ReactivateResource_ShapeValidation` (same three checks, no `Kind`).
  - `TestWriter_LifecycleRequest_NoContentField` — reflection assertion that neither `CatalogLifecycleRequest` nor `ResourceLifecycleRequest` declares `Values`, `Rules`, `Scope`, `Attributes`, or any other content field (spec scenario "No content field exists on a lifecycle request").
  - `TestWriter_NoUngraduatedMethodExported` (extended from Update's version): reflection over `WriteCapabilities`/`Writer` asserting exactly 8 methods, no `HardDelete` stub.
  - `TestExternalWrite_ConsumerDeactivatesAndReactivatesCatalogAndResourceUnderCAS` — a package-external fake `WriteCapabilities` implementing all 8 methods, proving `DeactivateCatalog`/`DeactivateResource` compile and succeed using only public types with `Active == false` returned (spec scenario "External consumer deactivates and reactivates a catalog record and a resource under CAS").
  - `TestExternalWrite_ConsumerReactivatesPreviouslyDeactivatedCatalogAndResource` — same fake, `ReactivateCatalog`/`ReactivateResource` return `Active == true` (spec scenario "External consumer reactivates a previously deactivated catalog record and resource").

  GREEN implements the smallest DTOs/`Writer` methods that satisfy shape validation only, with no business decision. TRIANGULATE covers `ID == 0` and `ExpectedRevision == 0` independently for both request types across all 4 methods, and blank `Actor` after `strings.TrimSpace`. REFACTOR keeps `Writer` free of any reference-existence, no-op-detection, or revision-currency decision — those stay internal.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` and record compile/full-suite success before U2.

### U2 — Catalog bridge translation, confirm-read, shared 6-category table

- [x] Widen `catalogWriter` in `internal/bridge/resourcecore/adapter.go` with `DeactivateRevision(ctx, kind, id, expectedRevision) error` and `ReactivateRevision(ctx, kind, id, expectedRevision) error`, bound to the already-production `catalogo.Service.DeactivateRevision`/`ReactivateRevision` (`service.go:598,603`); add `Adapter.DeactivateCatalog`/`Adapter.ReactivateCatalog` with the confirm-read (`a.catalog.Get` after a successful toggle), reusing `a.mapCatalogRecord`, `mapError`, `core.WithActor` unmodified — zero new mapping functions; adapt every in-repo bridge fake in the same slice. Add coverage in `internal/bridge/resourcecore/adapter_test.go`. Satisfies the catalog half of "Lifecycle field completeness through the sole bridge, including the catalog confirm-read," the catalog half of "Idempotent no-op / CAS-bypass asymmetry, reused and documented," "Lifecycle-reachable error category coverage" (catalog's 6-category table), and the catalog half of "Actor attribution without persistence on Deactivate/Reactivate." <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `internal/bridge/resourcecore/adapter.go` and `internal/bridge/resourcecore/adapter_test.go`.

**Dependencies and end state:** U1 is green. `catalogWriter` = existing `Create`/`UpdateRevision` + new `DeactivateRevision`/`ReactivateRevision`. `*catalogo.Service` already satisfies the widened port in production — only in-repo test fakes gain the two methods; `internal/tui/catalog_admin.go`'s existing `catalogDeactivator`/`catalogReactivator` interfaces (lines 75-81) already prove this exact call shape is a real, already-consumed production seam. `Adapter.DeactivateCatalog` calls `a.catalog.DeactivateRevision(core.WithActor(ctx, req.Actor), kind, req.ID, req.ExpectedRevision)`; on success it issues `rec, err := a.catalog.Get(ctx, kind, req.ID)` and returns `a.mapCatalogRecord(kind, rec)`; a confirm-read failure returns `mapError(err)` through the ordinary path, unreclassified — matching `CreateResource`'s established discipline (`adapter.go:229-234`). `ReactivateCatalog` is byte-identical except it calls `a.catalog.ReactivateRevision`. Field-completeness: `CatalogLifecycleRequest.Kind`→`kind` argument (Mapped), `.ID`→`id` argument (Mapped), `.ExpectedRevision`→`expectedRevision` argument (Mapped), `.Actor`→`core.WithActor` (Mapped, diagnostics-only). `internal/app/*`, `internal/domain/*`, `internal/postgres/*` remain unchanged. Rollback removes only these two files' catalog-lifecycle additions.

**Focused checks:** `go test ./internal/bridge/resourcecore -run 'TestCatalogLifecycle|TestCatalogDeactivate|TestCatalogReactivate' -count=1`.

**Strict evidence:** RED adds, before implementation, in `internal/bridge/resourcecore/adapter_test.go`:
  - `TestWriteBridge_CatalogWriter_DeactivateRevisionFakeAdapted` and `TestWriteBridge_CatalogWriter_ReactivateRevisionFakeAdapted` — every in-repo fake compiles against the widened port.
  - `TestCatalogLifecycle_FieldCompleteness_KindIDExpectedRevision` — the three fresh completeness rows against the `kind`/`id`/`expectedRevision` arguments (spec: "Every lifecycle field is mapped or documented").
  - `TestCatalogLifecycle_SixCategoryTable_DistinctAndNoLeakage` — injects each of the 6 catalog lifecycle-reachable sentinel families (`INVALID_ARGUMENT`, `NOT_FOUND`, `INVALID_CATALOG`, `CONFLICT`, `UNAVAILABLE`, `INTERNAL`) into the widened `catalogWriter` fake for both `DeactivateCatalog` and `ReactivateCatalog`, and asserts exact public code, distinctness, and no leakage (`Error()`, `%v`, `%+v`, concrete type, `errors.Unwrap`, recursive chain) of pgx/`PgError`/SQLSTATE/constraint/table/column/server text (spec: "Catalog Deactivate and Reactivate reach an identical 6-category set"). The `CONFLICT` row injects on a record in the *opposite* state from the requested toggle, so it is reachable outside the no-op path.
  - `TestCatalogLifecycle_SixCategoryTable_ExcludesUnreachable` — explicit assertion that `DUPLICATE`, `INVALID_REFERENCE`, `VALIDATION`, `INTEGRITY`, `IDENTITY_CONFLICT`, `REACTIVATION_IMPOSSIBLE`, `IN_USE`, and `IMMUTABLE_CODE` are never claimed reachable from either catalog lifecycle operation.
  - `TestCatalogLifecycle_ConfirmRead_ReturnsCompleteRecord` — after a successful fake `DeactivateRevision`/`ReactivateRevision` call, the fake `catalog.Get` returns a full record (all fields, not the 4-field partial projection); asserts the returned `public.CatalogRecord` carries every field, not just `Kind`/`ID`/`Revision`/`Active` (spec: "Catalog confirm-read returns the complete record after a successful toggle").
  - `TestCatalogLifecycle_ConfirmReadFailure_SurfacesHonestlyUnreclassified` — **the confirm-read failure-mode test.** After a successful fake `DeactivateRevision`/`ReactivateRevision`, the fake `catalog.Get` returns an injected error (e.g. a transient/unavailable sentinel); asserts the caller receives that `Get` failure's own mapped category, unmodified by any lifecycle-specific reclassification, and is never told the toggle itself failed (spec: "Catalog confirm-read failure surfaces honestly, unreclassified").
  - `TestCatalogDeactivate_NoOp_StaleRevision_SilentSuccess` — **asymmetry proof, leg 1.** Fake `catalogWriter.DeactivateRevision` records the record as already `Active == false` at revision 5; called with `ExpectedRevision` 3 (stale); asserts the call succeeds with no `CONFLICT` and the confirm-read returns the current record at revision 5 (spec: "Catalog Deactivate bypasses a stale revision when already deactivated").
  - `TestCatalogReactivate_NoOp_StaleRevision_SilentSuccess` — same shape for `ReactivateCatalog` against an already-`Active == true` record (spec: "Catalog Reactivate bypasses a stale revision when already active").
  - `TestCatalogLifecycle_ActorReachesDiagnosticSeam` — `Actor` reaches `internal/core/diagnostics.go`'s `core.WithActor`/`ActorFrom` seam on both the success and failure path for both operations; asserts `Actor` appears on no persisted field and no public DTO (spec: "Actor reaches diagnostics but never persistence on Deactivate/Reactivate").

  GREEN implements the seam widening, `Adapter.DeactivateCatalog`/`Adapter.ReactivateCatalog`, and adapts every in-repo fake. TRIANGULATE covers the no-op short-circuit boundary independently for Deactivate and Reactivate, the confirm-read success/failure boundary, and every reachable/unreachable category from the 6-category table. REFACTOR confirms zero new mapping functions were introduced (only the two `Adapter` methods and the two seam methods are new) and that no reference-existence or business decision entered the bridge.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` and record compile/full-suite success before U3.

### U3 — Resource bridge translation, both ops, 5- and 7-category tables

- [x] Widen `resourceWriter` in `internal/bridge/resourcecore/adapter.go` with `DeactivateRevision(ctx, id, expectedRevision) (domain.LifecycleResult, error)` and `ReactivateRevision(ctx, id, expectedRevision) (domain.LifecycleResult, error)`, bound to the already-production `recursos.Service.DeactivateRevision`/`ReactivateRevision` (`service.go:233,255`); add `Adapter.DeactivateResource`/`Adapter.ReactivateResource` with **no** confirm-read, mapping `LifecycleResult.Resource` directly through the existing `mapResource`; adapt every in-repo bridge fake in the same slice. Add coverage in `internal/bridge/resourcecore/adapter_test.go`. Satisfies the resource half of "Lifecycle field completeness through the sole bridge, including the catalog confirm-read" (the no-confirm-read scenario), the resource half of "Idempotent no-op / CAS-bypass asymmetry, reused and documented," "Lifecycle-reachable error category coverage" (resource's 5- and 7-category tables), and the resource half of "Actor attribution without persistence on Deactivate/Reactivate." <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `internal/bridge/resourcecore/adapter.go` and `internal/bridge/resourcecore/adapter_test.go`.

**Dependencies and end state:** U2 is green. `resourceWriter` = existing `Create`/`UpdateRevision` + new `DeactivateRevision`/`ReactivateRevision`. `*recursos.Service` already satisfies the widened port in production — only in-repo test fakes gain the two methods; `internal/tui/resource_editor.go`'s existing `resourceLifecycle` interface (lines 131-134) already proves this exact call shape is a real, already-consumed production seam. `Adapter.DeactivateResource` calls `result, err := a.resources.DeactivateRevision(core.WithActor(ctx, req.Actor), req.ID, req.ExpectedRevision)` and returns `mapResource(result.Resource)` on success / `mapError(err)` on failure — with no additional read call, because `domain.LifecycleResult.Resource` is already the complete, persisted resource. `ReactivateResource` is byte-identical except it calls `a.resources.ReactivateRevision`. Field-completeness: `ResourceLifecycleRequest.ID`→`id` argument (Mapped), `.ExpectedRevision`→`expectedRevision` argument (Mapped), `.Actor`→`core.WithActor` (Mapped, diagnostics-only). `internal/app/*`, `internal/domain/*`, `internal/postgres/*` remain unchanged. Rollback removes only these two files' resource-lifecycle additions.

**Focused checks:** `go test ./internal/bridge/resourcecore -run 'TestResourceLifecycle|TestResourceDeactivate|TestResourceReactivate' -count=1`.

**Strict evidence:** RED adds, before implementation, in `internal/bridge/resourcecore/adapter_test.go`:
  - `TestWriteBridge_ResourceWriter_DeactivateRevisionFakeAdapted` and `TestWriteBridge_ResourceWriter_ReactivateRevisionFakeAdapted` — every in-repo fake compiles against the widened port.
  - `TestResourceLifecycle_FieldCompleteness_IDAndExpectedRevision` — the two fresh completeness rows against the `id`/`expectedRevision` arguments for both operations.
  - `TestResourceLifecycle_NoConfirmRead_CallCountAssertion` — drives the `resources` fake to record a call count per method; asserts `Adapter.DeactivateResource` and `Adapter.ReactivateResource` each call their respective `*Revision` method exactly once and `Get`/`GetByID` exactly zero additional times beyond whatever the internal service itself already performs, on both the success path and every injected-category failure path (spec: "Resource lifecycle toggles need no confirm-read"). This resolves design.md's structural claim with a provable assertion, not prose alone.
  - `TestResourceDeactivate_FiveCategoryTable_DistinctAndNoLeakage` — injects each of the 5 resource-Deactivate-reachable sentinel families (`INVALID_ARGUMENT`, `NOT_FOUND`, `CONFLICT`, `UNAVAILABLE`, `INTERNAL`) into the widened `resourceWriter` fake and asserts exact public code, distinctness, and no leakage.
  - `TestResourceDeactivate_FiveCategoryTable_ExcludesUnreachable` and `TestResourceDeactivate_NeverReturnsIdentityConflict` — explicit assertions that `IDENTITY_CONFLICT`, `REACTIVATION_IMPOSSIBLE`, `DUPLICATE`, `INVALID_REFERENCE`, `VALIDATION`, and `INTEGRITY` are never claimed reachable from `DeactivateResource` (spec: "Resource Deactivate never returns IDENTITY_CONFLICT").
  - `TestResourceReactivate_SevenCategoryTable_DistinctAndNoLeakage` — injects each of the 7 resource-Reactivate-reachable sentinel families (`INVALID_ARGUMENT`, `NOT_FOUND`, `IDENTITY_CONFLICT`, `REACTIVATION_IMPOSSIBLE`, `CONFLICT`, `UNAVAILABLE`, `INTERNAL`) and asserts exact public code, distinctness, and no leakage.
  - `TestResourceReactivate_IdentityConflictAndReactivationImpossible_FirstReachable` — **the first-graduation proof.** Two injected paths per category: (a) fake `GetByID` returns a resource whose rebuilt identity mismatches the current one → `IDENTITY_CONFLICT`; (b) fake `ReactivateRevision` returns `domain.ErrResourceIntegrity` → `IDENTITY_CONFLICT`; (c) fake `GetByID` returns attributes that fail `domain.NewResource` reconstruction → `REACTIVATION_IMPOSSIBLE`; (d) fake `ReactivateRevision` returns `domain.ErrResourceReference` → `REACTIVATION_IMPOSSIBLE`. Asserts neither category was reachable from any previously graduated operation (spec: "Resource Reactivate is the first operation to reach IDENTITY_CONFLICT and REACTIVATION_IMPOSSIBLE").
  - `TestResourceReactivate_NoOp_StaleRevision_SilentSuccess` — **asymmetry proof, leg 2.** Fake `resourceWriter.ReactivateRevision`/`GetByID` records the resource as already `Active == true` at revision 5; called with `ExpectedRevision` 3 (stale); asserts the call succeeds with no `CONFLICT` and returns the current resource at revision 5 (spec: "Resource Reactivate bypasses a stale revision when already active").
  - `TestResourceDeactivate_SameCallShape_StaleRevision_Conflict` — **asymmetry proof, leg 3 (the contrast).** Same call shape as the leg-2 test above — an already-inactive resource at revision 5, called with `ExpectedRevision` 3 (stale) — but through `DeactivateResource`; asserts the caller receives `CONFLICT`, not silent success, and the persisted revision/`Active` value are unchanged (spec: "Resource Deactivate enforces CAS strictly even when already inactive"). This test, together with the two no-op tests above (this unit's leg 2, U2's catalog leg), is the falsifiable evidence for the entire documented asymmetry claim — not readiness prose alone.
  - `TestResourceLifecycle_ActorReachesDiagnosticSeam` — `Actor` reaches the diagnostic seam on both the success and failure path for both operations; asserts `Actor` appears on no persisted field and no public DTO.

  GREEN implements the seam widening, `Adapter.DeactivateResource`/`Adapter.ReactivateResource`, and adapts every in-repo fake. TRIANGULATE covers the no-op short-circuit boundary for Reactivate independently from Deactivate's strict-CAS boundary at the identical call shape, and every reachable/unreachable category from both tables. REFACTOR confirms zero new mapping functions were introduced and that `IDENTITY_CONFLICT`/`REACTIVATION_IMPOSSIBLE` arms required no `internal/core.Map` change (already correctly ordered, `internal/core/errors.go:74-77`).

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` and record compile/full-suite success before U4.

### U4 — Readiness record and full-suite verification

- [x] Create `openspec/changes/resource-master-core-write-lifecycle/readiness.md` recording the verdict, the three reachability tables (catalog shared 6, resource Deactivate 5, resource Reactivate 7 — with evidence pointers into design.md's and U2/U3's line-numbered tables), the confirm-read presence/absence proof for each side, and the full asymmetry documentation (all three legs of the proof pair, cross-referenced to their U2/U3 test names). No source change in this unit beyond the readiness document. Satisfies the zero-touch half of "Compiled surface extended to Create, Update, Deactivate, and Reactivate" and closes the readiness success criteria from proposal.md. <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `openspec/changes/resource-master-core-write-lifecycle/readiness.md`. No `.go` file is touched in this unit.

**Dependencies and end state:** U3 is green. The readiness record states: the verdict; catalog Deactivate/Reactivate's identical 6 proven-reachable categories with evidence pointers; resource Deactivate's 5 and resource Reactivate's 7, including the first-ever `IDENTITY_CONFLICT`/`REACTIVATION_IMPOSSIBLE` proof; the explicit exclusion list per operation (`DUPLICATE`, `INVALID_REFERENCE`, `VALIDATION`, `INTEGRITY`, `IN_USE`, `IMMUTABLE_CODE`, `INVALID_LIFECYCLE`, and the cross-operation exclusions of `IDENTITY_CONFLICT`/`REACTIVATION_IMPOSSIBLE` from the catalog and resource-Deactivate paths); the catalog confirm-read's presence and its failure-mode discipline (U2); the resource side's confirm-read absence, proven by call-count assertion (U3); the full no-op/CAS-bypass asymmetry, naming all three proof-pair tests (`TestCatalogDeactivate_NoOp_StaleRevision_SilentSuccess`/`TestCatalogReactivate_NoOp_StaleRevision_SilentSuccess` from U2, `TestResourceReactivate_NoOp_StaleRevision_SilentSuccess` and `TestResourceDeactivate_SameCallShape_StaleRevision_Conflict` from U3) and stating explicitly that the asymmetry is reused-as-is, not fixed; the disclosed live-PostgreSQL integration-evidence gap in a sandboxed apply session, matching Update's precedent; and zero-touch confirmation for `cmd/garfex`/`internal/tui`.

**Focused checks:** `go test ./... -count=1`; `rg -l resourcecore cmd/garfex internal/tui` and `git diff --stat -- cmd/garfex internal/tui` both empty; `gofmt -l .`, `go vet ./...`, `golangci-lint run ./...` all clean.

**Strict evidence:** RED confirms, before writing the readiness record, that the three reachability tables and the three-leg asymmetry proof are each backed by a named, already-green test from U2/U3 (re-grepped by test name, not repeated from memory). GREEN writes the readiness record citing those exact test names and design.md's line-numbered evidence. TRIANGULATE independently re-runs the full suite and the zero-touch scans rather than trusting U1–U3's prior receipts alone. REFACTOR simplifies the readiness record for reviewer scanning without weakening any claim.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` one final time and record compile/full-suite success before the parent-controlled gate.

## Parent-controlled gate after the final unit

- [x] G1 — Deactivate/Reactivate-WRITE readiness. After U4, recalculate Deactivate/Reactivate-WRITE readiness from `resourcecore/`, `internal/bridge/resourcecore/`, and `openspec/changes/resource-master-core-write-lifecycle/readiness.md`; confirm all of the following before treating Deactivate/Reactivate-WRITE as ready:
  - (a) an external Go package constructs a `Writer` and calls `DeactivateCatalog`/`DeactivateResource` and, separately, `ReactivateCatalog`/`ReactivateResource`, successfully under CAS with no `internal` import, and each returned record reports the correct `Active` value (spec: "Consumer-neutral public Deactivate/Reactivate for catalog and resource");
  - (b) neither `CatalogLifecycleRequest` nor `ResourceLifecycleRequest` declares a content field (spec scenario "No content field exists on a lifecycle request");
  - (c) every public lifecycle-request field is mapped at the bridge or carries a one-line omission rationale, for both catalog (`kind`/`id`/`expectedRevision` arguments) and resource (`id`/`expectedRevision` arguments) (spec: "Lifecycle field completeness through the sole bridge, including the catalog confirm-read");
  - (d) the catalog confirm-read returns a complete record on success and its own failure surfaces honestly and unreclassified, never masking a durably-committed toggle as a failed operation (spec scenarios "Catalog confirm-read returns the complete record after a successful toggle" and "Catalog confirm-read failure surfaces honestly, unreclassified");
  - (e) the resource side performs zero confirm-read calls — proven by U3's call-count assertion, not asserted only in prose (spec scenario "Resource lifecycle toggles need no confirm-read");
  - (f) catalog Deactivate and Reactivate each prove exactly the same 6 reachable and distinct categories, resource Deactivate proves exactly 5, and resource Reactivate proves exactly 7 — including the first-ever reach of `IDENTITY_CONFLICT`/`REACTIVATION_IMPOSSIBLE` — with every excluded category from the spec's "Lifecycle-reachable error category coverage" requirement confirmed never claimed reachable;
  - (g) all three legs of the no-op/CAS-bypass asymmetry proof pair pass and are named in the readiness record: catalog no-op silent success (both ops), resource Reactivate no-op silent success, and resource Deactivate's same-call-shape `CONFLICT` contrast — with the readiness record stating explicitly that this asymmetry is reused-as-is, not fixed (spec: "Idempotent no-op / CAS-bypass asymmetry, reused and documented");
  - (h) `Actor` reaches the internal diagnostic seam via `core.WithActor`/`core.ActorFrom` only, is never persisted, introduces no new column or migration, and appears on no public DTO, on both the success and failure path, for all 4 operations (spec: "Actor attribution without persistence on Deactivate/Reactivate");
  - (i) exactly `CreateCatalog`, `CreateResource`, `UpdateCatalog`, `UpdateResource`, `DeactivateCatalog`, `ReactivateCatalog`, `DeactivateResource`, and `ReactivateResource` — 8 methods — exist on `Writer`/`WriteCapabilities`, with no `HardDelete` stub (spec: "Compiled surface extended to Create, Update, Deactivate, and Reactivate");
  - (j) `cmd/garfex/` and `internal/tui/` show zero changed lines and zero new `resourcecore` references, confirmed by U4's textual-reference scan;
  - (k) lifecycle requests and returned records are defensively copied at both edges; a caller mutating either after the call changes nothing in flight or persisted (spec: "Consumer-neutral public Deactivate/Reactivate for catalog and resource");
  - (l) `go test ./... -count=1`, `gofmt -l .`, `go vet ./...`, and `golangci-lint run ./...` all pass; CI-only race/build results and the disclosed live-PostgreSQL integration-evidence gap are reported separately, as receipts, not as a gate substitute.

  Do not apply or plan `HardDelete` in this change; it remains a separate, later, per-operation-gated change with its own readiness evidence, per the spec's inherited "Failing evidence withdraws only affected write readiness" requirement and this change's own non-goals. The archived spec's "Compiled surface limited to graduated Create and Update" requirement is superseded and removed from the merged spec at archive time, per this change's spec delta. <!-- sdd-owner: parent -->

## Final graph self-audit

- Unit order is exactly public lifecycle contract (U1) → catalog bridge translation + confirm-read + shared 6-category table (U2) → resource bridge translation, both ops, 5- and 7-category tables (U3) → readiness + full-suite verification (U4), matching design.md's "Task-unit breakdown (auto-chain)" table exactly, with no unit boundary changed.
- No unit edits `internal/app/*`, `internal/domain/*`, or `internal/postgres/*` — unlike Update, no internal-layer fix is needed for this graduation; `internal/core/errors.go`'s `Map` is never edited, since `IdentityConflict`/`ReactivationImpossible` already exist and are already correctly ordered.
- Every port widening (`catalogWriter` in U2, `resourceWriter` in U3) adapts every in-repo fake in the same slice; no port is edited without every current implementation and test double adapting together.
- `core.Record`'s, `core.WithActor`'s, and `DiagnosticSink`'s signatures never change across the chain — Create/Update's mechanism is reused as-is, with zero redesign.
- Every unit has a concrete start dependency, exact edit surfaces, focused RED/GREEN/TRIANGULATE/REFACTOR evidence, a rollback boundary, and mandatory `go test ./... -count=1` compile/full-suite proof.
- The design's structural novelty — the catalog-side confirm-read versus the resource-side's proven absence of one — is resolved by named, concrete tests in U2 (`TestCatalogLifecycle_ConfirmRead_ReturnsCompleteRecord`, `TestCatalogLifecycle_ConfirmReadFailure_SurfacesHonestlyUnreclassified`) and U3 (`TestResourceLifecycle_NoConfirmRead_CallCountAssertion`), not left as prose-only claims.
- The no-op/CAS-bypass asymmetry's three-test proof pair — `TestCatalogDeactivate_NoOp_StaleRevision_SilentSuccess`/`TestCatalogReactivate_NoOp_StaleRevision_SilentSuccess` (U2), `TestResourceReactivate_NoOp_StaleRevision_SilentSuccess` (U3), and `TestResourceDeactivate_SameCallShape_StaleRevision_Conflict` (U3) — is the falsifiable evidence for the entire documented asymmetry, named explicitly in the readiness record (U4), not restated as prose alone.
- The "no `Clone*` needed" decision is a named non-task inside U1, with its rationale (scalar-only fields, Go value semantics) recorded at the point where the omission could otherwise look like an oversight.
- The graph contains no `HardDelete` implementation task; it remains future work after its own later, separate, most-destructive-last per-operation readiness decision, exactly as proposal.md's non-goals require.
- Only `openspec/changes/resource-master-core-write-lifecycle/tasks.md`, the six `resourcecore`/`internal/bridge/resourcecore` source-and-test surfaces named above, `openspec/changes/resource-master-core-write-lifecycle/readiness.md`, and the corresponding Engram topic are affected by this plan; the archived Create and Update artifacts and any unrelated in-flight change remain untouched.
