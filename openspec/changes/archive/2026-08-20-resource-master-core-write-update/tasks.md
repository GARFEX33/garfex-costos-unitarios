# Implementation Tasks: Expose Public WRITE Update on the Resource Master Core

This is the strictly ordered auto-chain for the Update graduation of public WRITE. It builds directly on Create-WRITE (shipped, archived `2026-08-21-resource-master-core-write`) and design.md's fully worked "Task-unit breakdown (auto-chain)" table (U1/U2/U3). Per-operation graduation is inherited and binding from the archived spec's "Separate read and per-operation write readiness" requirement and is not reopened here. `Deactivate`, `Reactivate`, and `HardDelete` remain designed on paper only; no unit in this plan compiles, exports, or stubs any of them.

Catalog `UpdateCatalog` MUST prove exactly **10** reachable and distinct public error categories (not the explore.md-estimated 11 — `INTEGRITY` is not reachable from any catalog admin repository code path, grep-confirmed in design.md). Resource `UpdateResource` MUST prove exactly **9**. Both counts are fixed by `specs/resource-master-core/spec.md`'s "Update-reachable error category coverage" requirement and must not drift back toward the original estimate.

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | ~900–1,100 total per explore.md's estimate (2 methods, reused mappers — smaller than Create's 1,348 lines for 7 methods, but the two full reachability tables and their injected tests remain substantial per-unit evidence) |
| 400-line budget risk | High (per proposal.md's Risks table) |
| Chained PRs recommended | Yes |
| Suggested split | U1 → U2 → U3 |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

Each unit is one stacked-to-main review boundary with its tests, focused receipt, rollback boundary, and full-suite proof. No unit may exceed 400 additions plus deletions. If measured work exceeds its forecast, split that unit along design.md's own file/table granularity; do not create a broad exception and do not improvise a new boundary.

### Per-slice forecast

| Slice | Forecast |
| ------- | ---------- |
| U1 | ≤400 changed lines |
| U2 | ≤400 changed lines |
| U3 | ≤400 changed lines |

Every unchecked implementation task below maps one-to-one to this forecast table.

## Global execution and compatibility contract

- Use strict RED → GREEN → TRIANGULATE → REFACTOR evidence for every unit that touches a package with tests. RED must be captured before the production behavior is implemented.
- For every slice, run the unit's exact focused `-run` command first, then the mandatory boundary command `go test ./... -count=1`; the latter is both the compile-safety proof for every current adapter, service, composition root, TUI call site, test double, and external test, and the full-suite proof. Record the exact command and result in the unit receipt.
- Per the `GARFEX_STRICT` focused-test discoverability gate (`.agents/skills/golang-hexagonal/references/garfex-strict-profile.md`): run the exact stated `-run` command, confirm it selects at least one test in every package the unit touched (`ok ... [no tests to run]` for a touched package is a failed gate even though `go test` exits 0), and name every new test function so at least one literal alternative from that unit's `-run` pattern appears as a substring of the function name after `Test`.
- Per the field-completeness gate: diff every public Update-request field against its internal destination field-by-field at the bridge; an omission needs either a mapping or a one-line rationale comment at the omission site, never silence. A public field the domain cannot honor is `MISSING DOMAIN CRITERION` — a BLOCKER that drops the field from the public contract, never accept-and-ignore.
- Never widen `catalogWriter`/`resourceWriter` without adapting every in-repo fake in the same slice.
- Never change `core.Record`'s, `core.WithActor`'s, or `DiagnosticSink`'s existing signature. `Actor` travels only via `core.WithActor(ctx, ...)` / `core.ActorFrom(ctx)`, reusing Create's mechanism as-is; never a domain or application function parameter; never persisted; never given a new column or migration; never exposed on a public DTO.
- `ExpectedRevision == 0` and `ID == 0` are both `INVALID_ARGUMENT` at the `Writer` shape gate for either request — CAS is mandatory; there is no "unconditional update" request shape.
- `internal/app/*` and `internal/domain/*` stay at zero changed lines for the entire chain. `internal/postgres/*` changes exactly the two-line CONFLICT-sentinel reassignment in U3 — no other postgres file changes. `internal/core/errors.go`'s `Map` is unchanged — the `Conflict`/`ImmutableCode` arms already exist and are already correctly ordered.
- No unit compiles, exports, or stubs `Deactivate`, `Reactivate`, or `HardDelete` as a symbol, an "unimplemented" branch, an unused request field, or any other runtime-discoverable artifact. `CatalogWriteRequest`/`ResourceWriteRequest` (Create) stay untouched; Update is purely additive.
- The READ contract and Create's compiled WRITE surface stay byte-compatible: no read DTO, method, or error identity changes in any unit; no Create method, field, or error identity changes in any unit.
- `Adapter.UpdateResource` must perform **no create-confirm read** — `domain.ResourceRepositoryV2.UpdateRevision` already returns the real, persisted `domain.Resource`. This absence is a provable claim, not a documentation-only one: U2 must carry a call-count assertion on the fake proving `resources.Get` is never invoked by `Adapter.UpdateResource`.
- `IMMUTABLE_CODE` evidence must go through the kind registry (`domain.NewCatalogRegistry().Kinds()` / `.Kind(code)`, `FieldDescriptor.Immutable == domain.ImmutableOnceReferenced`), never a hardcoded single kind literal, so a future kind adding a second immutable field is automatically covered.
- Inspect `git diff --name-only` and `git diff --stat` before and after every unit. Preserve unrelated user changes and preserve every other in-flight `openspec/changes/*` directory without edits.
- Do not run local `go build`, `docker build`, or blanket database/volume cleanup. U1 and U2 use in-repo fakes for `catalogWriter`/`resourceWriter`, not a live database; U3's postgres verification reuses the existing integration-test suite unmodified.
- When work cannot fit in 400 changed lines, split into another additive unit along design.md's own file/table granularity rather than weakening these rules.

## Ordered implementation units

### U1 — Public Update contract

- [x] Add `CatalogUpdateRequest`, `ResourceUpdateRequest` to `resourcecore/write_types.go` (additive; `CatalogWriteRequest`/`ResourceWriteRequest` untouched); add `Writer.UpdateCatalog`/`Writer.UpdateResource`, `WriteCapabilities` +2 methods, `validateCatalogUpdateRequest`/`validateResourceUpdateRequest` (reusing `validateValueShape`/`isKnownKind`/`isKnownAttributeMode` verbatim, zero duplication) to `resourcecore/writer.go`; add `CloneCatalogUpdateRequest`/`CloneResourceUpdateRequest` to `resourcecore/copy.go`, built on the same `CloneValue`/`CloneStringSlice` primitives as the Create clones; extend `resourcecore/writer_test.go` and `resourcecore/external_test.go`. Satisfies spec requirements "Consumer-neutral public Update for catalog and resource" and "Compiled surface limited to graduated Create and Update." <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `resourcecore/write_types.go`, `resourcecore/writer.go`, `resourcecore/copy.go`, `resourcecore/writer_test.go`, and `resourcecore/external_test.go`.

**Dependencies and end state:** start from Create-WRITE shipped and archived; `Reader`/`ReadCapabilities` and `CatalogWriteRequest`/`ResourceWriteRequest`/Create's `Writer` methods remain untouched byte-for-byte. `Writer.UpdateCatalog`/`UpdateResource` validate request shape only: blank `Actor` → `INVALID_ARGUMENT` (unchanged rule); `ID == 0` → `INVALID_ARGUMENT` (mirrors `catalogo.Service.UpdateRevision`'s `rec.ID == 0` guard, `service.go:526`, and `recursos.Service.UpdateRevision`'s `command.ID <= 0` guard, `service.go:301`); `ExpectedRevision == 0` → `INVALID_ARGUMENT` (mirrors both services' `expectedRevision == 0` guard); catalog unknown `Kind`, empty `Values`, per-`Value`/per-`Rule` shape identical to `validateCatalogWriteRequest` (Update is full-replacement, same non-empty-`Values` rule as Create); resource blank `Scope.ClassCode`/`FamilyCode`/`TypeCode`/`NaturalUnit` and per-`AttributeValue` shape identical to `validateResourceWriteRequest`. No business rule (reference existence, uniqueness, immutability, revision currency) is pre-judged at the boundary. `WriteCapabilities` compiles exactly 4 methods (`CreateCatalog`, `CreateResource`, `UpdateCatalog`, `UpdateResource`); no ungraduated method exists as a symbol, stub, or discoverable artifact. Requests and returned records are deep-copied at both edges, preserving nil-versus-empty for `Values`/`Rules`/`Attributes` identically to Create's clones. Nothing implements `WriteCapabilities` in production yet with the widened 4-method shape — no bridge or composition wiring is affected by this unit; `internal/bridge/resourcecore.Adapter` is untouched until U2. Rollback removes only these five `resourcecore` files' Update additions.

**Focused checks:** `go test ./resourcecore -run 'TestWriter|TestExternalWrite|TestWriteRequestCopy' -count=1`.

**Strict evidence:** RED adds, before implementation: `TestWriter_UpdateCatalog_ShapeValidation` (`ID == 0`, `ExpectedRevision == 0`, blank `Actor`, unknown `Kind`, empty `Values`, per-`Value`/per-`Rule` shape — same table shape as Create's catalog test, extended with the two CAS/identity checks); `TestWriter_UpdateResource_ShapeValidation` (`ID == 0`, `ExpectedRevision == 0`, blank `Scope` fields, per-`AttributeValue` shape); `TestWriter_NoUngraduatedMethodExported` (reflection over `WriteCapabilities`/`Writer`, extended to assert exactly 4 methods, no 5th ungraduated one for `Deactivate`/`Reactivate`/`HardDelete`); `TestWriteRequestCopy_CallerMutationAfterCall_NoEffect` extended to `CatalogUpdateRequest`/`ResourceUpdateRequest` (spec scenario "Caller mutation after the call has no effect"); and `TestExternalWrite_ConsumerUpdatesCatalogAndResourceUsingPublicTypesOnly` (a package-external fake `WriteCapabilities` implementing all 4 methods, proving `UpdateCatalog`/`UpdateResource` compile and succeed using only public types, with the returned `Revision` greater than the supplied `ExpectedRevision` — spec scenario "External consumer updates catalog and resource records under CAS"). GREEN implements the smallest DTOs/`Writer` methods/copies that satisfy shape validation only, with no business decision. TRIANGULATE covers `ID == 0` and `ExpectedRevision == 0` independently for both request types, nil-vs-empty `Values`/`Rules`/`Attributes`, and blank `Actor` after `strings.TrimSpace`. REFACTOR keeps `Writer` free of any reference-existence, catalog-validity, uniqueness, or revision-currency decision.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` and record compile/full-suite success before U2.

### U2 — Bridge Update translation (catalog + resource)

- [x] Widen `catalogWriter`/`resourceWriter` in `internal/bridge/resourcecore/adapter.go` with one `UpdateRevision` method each, bound to the already-production `catalogo.Service.UpdateRevision` (`service.go:525`) and `recursos.Service.UpdateRevision` (`service.go:300`); add `Adapter.UpdateCatalog`/`Adapter.UpdateResource`, reusing every existing inverse mapper (`a.toDomainCatalogValues`, `toDomainCatalogRules`, `toDomainResourceAttributes`, `a.mapCatalogRecord`, `mapResource`, `mapError`, `core.WithActor`) unmodified — zero new mapping functions; adapt every in-repo bridge fake in the same slice. Add coverage in `internal/bridge/resourcecore/adapter_test.go`. Satisfies spec requirements "Update field completeness through the sole bridge," "Catalog stale-revision update returns CONFLICT, not INTERNAL" (injected-CONFLICT half — the postgres fix itself lands in U3), "Update-reachable error category coverage," and "Actor attribution without persistence on Update." <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `internal/bridge/resourcecore/adapter.go` and `internal/bridge/resourcecore/adapter_test.go`.

**Dependencies and end state:** U1 is green. `catalogWriter` = existing `Create` + new `UpdateRevision(ctx, kind, rec, expectedRevision) (domain.CatalogRecord, error)`; `resourceWriter` = existing `Create` + new `UpdateRevision(ctx, command, expectedRevision) (domain.Resource, error)`. `*catalogo.Service` and `*recursos.Service` already satisfy the widened ports in production — only in-repo test fakes gain the method; `internal/tui/catalog_admin.go`'s existing `catalogRecordUpdater` interface already proves this exact `UpdateRevision` call shape is a real, already-consumed production seam, not an invented one. `Adapter.UpdateCatalog` builds `domain.CatalogRecord{ID, Active, Values, Rules}` from the request, calls `a.catalog.UpdateRevision(core.WithActor(ctx, req.Actor), kind, rec, req.ExpectedRevision)`, and on success returns `a.mapCatalogRecord(kind, updated)`; on failure returns `mapError(err)` unchanged. `Adapter.UpdateResource` builds `domain.UpdateCommand{ID, Scope, NaturalUnit, Attributes}`, calls `a.resources.UpdateRevision(core.WithActor(ctx, req.Actor), command, req.ExpectedRevision)`, and returns `mapResource(updated)` on success / `mapError(err)` on failure — **with no confirm-read call**, because `ResourceRepositoryV2.UpdateRevision` already returns the real persisted `domain.Resource` (unlike the legacy `resourceRepository.Create` gap `CreateResource` must work around). Field-completeness: `CatalogUpdateRequest.ID`→`rec.ID` (Mapped), `CatalogUpdateRequest.ExpectedRevision`→`UpdateRevision`'s `expectedRevision` argument (Mapped), `ResourceUpdateRequest.ID`→`domain.UpdateCommand.ID` (Mapped), `ResourceUpdateRequest.ExpectedRevision`→`recursos.Service.UpdateRevision`'s `expectedRevision` argument (Mapped); every other field's mapping is identical to Create's already-proven table, re-verified by a table-driven inverse round-trip test that exercises the same mapping functions, not new ones. `internal/app/*`, `internal/domain/*`, `internal/postgres/*` remain unchanged. Rollback removes only these two files' Update additions.

**Focused checks:** `go test ./internal/bridge/resourcecore -run 'TestWriteBridge|TestCatalogUpdate|TestResourceUpdate' -count=1`.

**Strict evidence:** RED adds, before implementation, in `internal/bridge/resourcecore/adapter_test.go`:
  - `TestWriteBridge_CatalogWriter_UpdateRevisionFakeAdapted` and `TestWriteBridge_ResourceWriter_UpdateRevisionFakeAdapted` — every in-repo fake compiles against the widened ports.
  - `TestCatalogUpdate_FieldCompleteness_IDAndExpectedRevision` and `TestResourceUpdate_FieldCompleteness_IDAndExpectedRevision` — the two fresh completeness rows against `domain.CatalogRecord`/`UpdateRevision`'s `expectedRevision` argument and `domain.UpdateCommand`/`recursos.Service.UpdateRevision`'s `expectedRevision` argument.
  - `TestCatalogUpdate_TenCategoryTable_DistinctAndNoLeakage` — injects each of the 10 catalog Update-reachable sentinel families (`INVALID_ARGUMENT`, `NOT_FOUND`, `DUPLICATE`, `INVALID_REFERENCE`, `VALIDATION`, `INVALID_CATALOG`, `UNAVAILABLE`, `INTERNAL`, `CONFLICT`, `IMMUTABLE_CODE`) into the widened `catalogWriter` fake and asserts exact public code, distinctness from every other reachable category, and no leakage (`Error()`, `%v`, `%+v`, concrete type, `errors.Unwrap`, recursive chain) of pgx/`PgError`/SQLSTATE/constraint/table/column/server text. The `CONFLICT` row injects `domain.ErrRevisionConflict` directly at this seam (the postgres sentinel wiring itself is U3's job; this table proves the bridge classifies it correctly once it arrives).
  - `TestResourceUpdate_NineCategoryTable_DistinctAndNoLeakage` — same shape for the 9 resource Update-reachable categories (`INVALID_ARGUMENT`, `NOT_FOUND`, `DUPLICATE`, `INVALID_REFERENCE`, `VALIDATION`, `INTEGRITY`, `UNAVAILABLE`, `INTERNAL`, `CONFLICT`).
  - `TestCatalogUpdate_TenCategoryTable_ExcludesUnreachable` and `TestResourceUpdate_NineCategoryTable_ExcludesUnreachable` — explicit assertions that `IDENTITY_CONFLICT`, `INVALID_LIFECYCLE`, `REACTIVATION_IMPOSSIBLE`, and `IN_USE` are never claimed reachable from either operation, `IMMUTABLE_CODE` is never claimed reachable from `UpdateResource`, and `INTEGRITY` is never claimed reachable from `UpdateCatalog` (spec: "Update-reachable error category coverage," scenario "Update reaches categories Create could not").
  - `TestResourceUpdate_NoConfirmRead_CallCountAssertion` — **resolves design.md's open question 1.** Drives the `resources` fake to record a call count per method; asserts `Adapter.UpdateResource` calls `UpdateRevision` exactly once and `Get` exactly zero times, on both the success path and every one of the 9 injected-category failure paths. The create-confirm-read pattern's *absence* is proven by this assertion, not asserted only in prose.
  - `TestCatalogUpdate_ImmutableCode_ViaKindRegistryLookup` — **resolves design.md's open question 2.** Iterates `domain.NewCatalogRegistry().Kinds()`, locates the field(s) with `FieldDescriptor.Immutable == domain.ImmutableOnceReferenced` per kind (currently `code` on every kind, per `catalog_kind.go:128`), and drives the `IMMUTABLE_CODE` injected case from that registry lookup rather than a hardcoded kind literal, so a future kind adding a second immutable field is automatically covered by this test without modification.
  - `TestCatalogUpdate_ActorReachesDiagnosticSeam` / `TestResourceUpdate_ActorReachesDiagnosticSeam` — `Actor` reaches `internal/core/diagnostics.go`'s `core.WithActor`/`ActorFrom` seam on both the success and failure path (spec scenario "Actor reaches diagnostics but never persistence on Update"); asserts `Actor` appears on no persisted field and no public DTO.

  GREEN implements the seam widening, `Adapter.UpdateCatalog`/`Adapter.UpdateResource`, and adapts every in-repo fake. TRIANGULATE covers `ExpectedRevision` passthrough distinctness from `CONFLICT`'s injected-sentinel path, nil-vs-empty `Rules`/`Attributes` round-tripping through the reused mappers, and every reachable/unreachable category boundary from the two tables above. REFACTOR confirms no reference-existence, uniqueness, or business decision entered the bridge, and that zero new mapping functions were introduced (only the two `Adapter` methods and the two seam methods are new).

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` and record compile/full-suite success before U3.

### U3 — CONFLICT-wiring fix + readiness

- [x] Reassign `errApplicabilityStaleRevision` (line 20) and `errCatalogStaleRevisionV2` (lines 193–196) in `internal/postgres/catalog_admin_repository_v2.go` to `domain.ErrRevisionConflict`, exactly per design.md's before/after; run the full, unmodified `catalog_admin_repository_v2_integration_test.go` suite to confirm zero breakage across its 14 `errors.Is` call sites (lines 111, 407, 414, 603, 746, 753, 1027, 1037, 1047); create the Update readiness record at `openspec/changes/resource-master-core-write-update/readiness.md`. Satisfies spec requirement "Catalog stale-revision update returns CONFLICT, not INTERNAL" (the postgres-wiring half) and closes the readiness success criteria from proposal.md. <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `internal/postgres/catalog_admin_repository_v2.go` (the two-line-plus-comment sentinel reassignment only — no other change in this file) and `openspec/changes/resource-master-core-write-update/readiness.md`. `internal/postgres/catalog_admin_repository_v2_integration_test.go` is read/run, never edited.

**Dependencies and end state:** U2 is green. The reassignment is a direct value substitution, not a wrap: `errApplicabilityStaleRevision = domain.ErrRevisionConflict` and `errCatalogStaleRevisionV2 = domain.ErrRevisionConflict`, per design.md's "Decision: direct reassignment, not `fmt.Errorf`-wrapping" (every existing reference compares exclusively via `errors.Is`, never `==` or `.Error()` string content; non-test production code returns each sentinel directly, never wrapped). After this change, `casUpdateRevision`'s present-but-different-revision branch (`catalog_admin_repository_v2.go:224`) and `updateApplicabilityAggregateV2`'s parallel APLICABILIDAD path (line 131) both resolve through `core.Map`'s already-existing `Conflict` arm (`internal/core/errors.go:72-73`) to public `CONFLICT` instead of falling through to `default`/`INTERNAL`. `internal/core.Map` and `internal/domain` are unchanged — no new abstraction. The readiness record states: the verdict; catalog Update's 10 proven-reachable categories with evidence pointers into U2/U3 (citing design.md's reachability table's line numbers); resource Update's 9 proven-reachable categories; the explicit exclusion of `IDENTITY_CONFLICT`/`INVALID_LIFECYCLE`/`REACTIVATION_IMPOSSIBLE`/`IN_USE` from both operations, `IMMUTABLE_CODE` from `UpdateResource`, and `INTEGRITY` from `UpdateCatalog`; the resolution of design.md's two open questions (U2's call-count proof for no confirm-read; U2's kind-registry-driven `IMMUTABLE_CODE` evidence); the discrepancy with explore.md's original 11-category catalog estimate, stated honestly per design.md; zero-breakage confirmation for the 14 existing `errors.Is` integration-test assertions; and zero-touch confirmation for `cmd/garfex`/`internal/tui`. Rollback of this unit alone restores the prior `INTERNAL` misclassification for catalog `CONFLICT` — no persisted data or migration is touched by either the fix or its rollback.

**Focused checks:** `go test ./internal/postgres -run 'TestCatalogAdminRepositoryV2|TestApplicability' -count=1` (existing tests, unmodified, must stay green) plus the full suite `go test ./... -count=1`; `rg -l resourcecore cmd/garfex internal/tui` and `git diff --stat -- cmd/garfex internal/tui` both empty.

**Strict evidence:** RED confirms, before the reassignment, that the two-line-plus-comment change is the complete diff (no other line in `catalog_admin_repository_v2.go` changes) and that the 14 enumerated `errors.Is` call sites are the complete set of existing references to either sentinel (re-grepped, not repeated from memory). GREEN applies exactly the two reassignments and the updated comment on `errCatalogStaleRevisionV2`, per design.md's before/after text. TRIANGULATE re-runs the full existing integration-test suite and independently re-verifies (not merely re-states) U2's 10-category and 9-category tables and the two open-question resolutions before the readiness record cites them. REFACTOR simplifies the readiness record for reviewer scanning without weakening any claim; no `.go` file changes in this pass beyond the two-line-plus-comment fix already applied in GREEN.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` one final time and record compile/full-suite success before the parent-controlled gate.

## Parent-controlled gate after the final unit

- [x] G1 — Update-WRITE readiness. After U3, recalculate Update-WRITE readiness from `resourcecore/`, `internal/bridge/resourcecore/`, `internal/postgres/catalog_admin_repository_v2.go`, and `openspec/changes/resource-master-core-write-update/readiness.md`; confirm all of the following before treating Update-WRITE as ready:
  - (a) an external Go package constructs a `Writer` and calls both `UpdateCatalog` and `UpdateResource` successfully under CAS with no `internal` import, and each returned record's `Revision` is greater than the `ExpectedRevision` supplied (spec: "Consumer-neutral public Update for catalog and resource");
  - (b) every public Update-request field is mapped at the bridge or carries a one-line omission rationale, for both catalog (`domain.CatalogRecord` + `expectedRevision`) and resource (`domain.UpdateCommand` + `expectedRevision`) (spec: "Update field completeness through the sole bridge");
  - (c) a stale catalog `expectedRevision` returns `CONFLICT`, not `INTERNAL`, with no PostgreSQL detail leaked, and resource `CONFLICT` is confirmed already correct and unaffected (spec: "Catalog stale-revision update returns CONFLICT, not INTERNAL");
  - (d) catalog Update proves exactly 10 reachable and distinct categories and resource Update proves exactly 9, with `IDENTITY_CONFLICT`/`INVALID_LIFECYCLE`/`REACTIVATION_IMPOSSIBLE`/`IN_USE` never claimed reachable from either operation, `IMMUTABLE_CODE` never claimed reachable from `UpdateResource`, and `INTEGRITY` never claimed reachable from `UpdateCatalog` (spec: "Update-reachable error category coverage");
  - (e) `IMMUTABLE_CODE` is reachable and proven via the kind registry, not a hardcoded kind, and a referenced record's `code` value stays unchanged on rejection (spec: "Update-reachable error category coverage," scenario "Code change on a referenced catalog record is immutable");
  - (f) `Actor` reaches the internal diagnostic seam via `core.WithActor`/`core.ActorFrom` only, is never persisted, introduces no new column or migration, and appears on no public DTO, on both the success and failure path (spec: "Actor attribution without persistence on Update");
  - (g) `Adapter.UpdateResource` performs zero calls to `resources.Get` — proven by U2's call-count assertion, not asserted only in prose;
  - (h) update requests and returned records are defensively copied at both edges; a caller mutating either after the call changes nothing in flight or persisted (spec: "Consumer-neutral public Update for catalog and resource," scenario "Caller mutation after the call has no effect");
  - (i) `cmd/garfex/` and `internal/tui/` show zero changed lines and zero new `resourcecore` references, confirmed by U3's textual-reference scan (spec: "Compiled surface limited to graduated Create and Update");
  - (j) only `CreateCatalog`/`CreateResource`/`UpdateCatalog`/`UpdateResource` exist on `Writer`/`WriteCapabilities`, with no ungraduated stub for `Deactivate`/`Reactivate`/`HardDelete`;
  - (k) `go test ./... -count=1`, `gofmt -l .`, `go vet ./...`, and `golangci-lint run ./...` all pass; CI-only race/build results are reported separately, as receipts, not as a gate substitute.

  Do not apply or plan `Deactivate`, `Reactivate`, or `HardDelete` in this change; each remains a separate, later, per-operation-gated change with its own readiness evidence, per the archived spec's "Failing evidence withdraws only affected write readiness" and this change's own non-goals. <!-- sdd-owner: parent -->

## Final graph self-audit

- Unit order is exactly public Update contract (U1) → bridge Update translation, catalog + resource (U2) → CONFLICT-wiring fix + readiness (U3), matching design.md's "Task-unit breakdown (auto-chain)" table exactly, with no unit boundary changed.
- No unit edits `internal/app/*` or `internal/domain/*`; `internal/postgres/*` changes exactly the two-line-plus-comment CONFLICT fix in U3, and no other file in that package; U3 is the explicit, textual-reference-scanned proof for the `cmd/garfex`/`internal/tui` zero-touch half.
- `internal/core/errors.go`'s `Map` is never edited; the `Conflict`/`ImmutableCode` arms already exist and are reused as-is.
- Every port widening (`catalogWriter`/`resourceWriter` in U2) adapts every in-repo fake in the same slice; no port is edited without every current implementation and test double adapting together.
- `core.Record`'s, `core.WithActor`'s, and `DiagnosticSink`'s signatures never change across the chain — Create's mechanism is reused as-is, with zero redesign.
- Every unit has a concrete start dependency, exact edit surfaces, focused RED/GREEN/TRIANGULATE/REFACTOR evidence, a rollback boundary, and mandatory `go test ./... -count=1` compile/full-suite proof.
- Design.md's two open questions are each resolved by a named, concrete test in U2 (`TestResourceUpdate_NoConfirmRead_CallCountAssertion` for the confirm-read absence; `TestCatalogUpdate_ImmutableCode_ViaKindRegistryLookup` for the kind-registry-driven `IMMUTABLE_CODE` evidence), not left as prose-only claims.
- The graph contains no `Deactivate`/`Reactivate`/`HardDelete` implementation tasks; each remains future work after its own later, separate per-operation readiness decision, exactly as proposal.md's non-goals require.
- Only `openspec/changes/resource-master-core-write-update/tasks.md`, the seven `resourcecore`/`internal/bridge/resourcecore`/`internal/postgres` source-and-test surfaces named above, `openspec/changes/resource-master-core-write-update/readiness.md`, and the corresponding Engram topic are affected by this plan; the archived `2026-08-21-resource-master-core-write` artifacts and any unrelated in-flight change remain untouched.
