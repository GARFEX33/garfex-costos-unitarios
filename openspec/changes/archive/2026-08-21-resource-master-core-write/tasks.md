# Implementation Tasks: Expose Public WRITE on the Resource Master Core (Create)

This is the strictly ordered auto-chain for Create-only public WRITE. It builds directly on the accepted READ-ONLY `resourcecore` baseline (`stabilize-resource-master-core`, archived) and the fully designed `design.md` slice-transition table (W1/W2/W3). Per-operation graduation is inherited and binding from the archived spec's "Separate read and per-operation write readiness" requirement and is not reopened here. `Update`, `Deactivate`, `Reactivate`, and `HardDelete` are designed on paper in `design.md` only; no unit in this plan compiles, exports, or stubs any of them.

Unit W4 below splits design.md's single "W3 — resource Create bridge + readiness" row along the design's own file/table granularity (`readiness.md` is already a distinct row in design.md's "Files and change surfaces" table), because the combined resource-bridge implementation plus readiness/documentation/zero-reference evidence risks exceeding the 400-line review budget that design.md's own review-guard forecast flags as "High" risk. No other unit boundary in design.md is changed.

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | 600–1,400+ authored changed lines total (proposal.md anchor: 600–1,000+ for the narrowest Create-only slice, citing the READ-ONLY precedent's 1,348 lines for 7 methods); each implementation unit is forecast at 200–400 changed lines |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | W1 → W2 → W3 → W4 |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

Each unit is one stacked-to-main review boundary with its tests, focused receipt, rollback boundary, and full-suite proof. No unit may exceed 400 additions plus deletions. If measured work exceeds its forecast, split that unit along the design's own file/table granularity; do not create a broad exception and do not improvise a new boundary.

### Per-slice forecast

| Slice | Forecast |
| ------- | ---------- |
| W1 | ≤400 changed lines |
| W2 | ≤400 changed lines |
| W3 | ≤400 changed lines |
| W4 | ≤200 changed lines |

Every unchecked implementation task below maps one-to-one to this forecast table.

## Global execution and compatibility contract

- Use strict RED → GREEN → TRIANGULATE → REFACTOR evidence for every unit that touches a package with tests. RED must be captured before the production behavior is implemented. W4 adds no new Go test names; its evidence is documentation-review plus full-suite/lint/vet re-verification.
- For every slice, run the unit's exact focused `-run` command first, then the mandatory boundary command `go test ./... -count=1`; the latter is both the compile-safety proof for every current adapter, service, composition root, TUI call site, test double, and external test, and the full-suite proof. Record the exact command and result in the unit receipt.
- Per the `GARFEX_STRICT` focused-test discoverability gate (`.agents/skills/golang-hexagonal/references/garfex-strict-profile.md`): run the exact stated `-run` command, confirm it selects at least one test in every package the unit touched (`ok ... [no tests to run]` for a touched package is a failed gate even though `go test` exits 0), and name every new test function so at least one literal alternative from that unit's `-run` pattern appears as a substring of the function name after `Test`. Every test name listed in this plan's "Strict evidence" sections was checked against this rule before being written down.
- Per the field-completeness and query-completeness gates: diff every public request field against its internal destination field-by-field at the bridge; an omission needs either a mapping or a one-line rationale comment at the omission site, never silence. A public field the domain cannot honor is `MISSING DOMAIN CRITERION` — a BLOCKER that drops the field from the public contract, never accept-and-ignore (the `ResourceQuery.TypeCode` precedent must not repeat on write).
- Never widen `catalogPort`/`resourcePort` without adapting every in-repo fake in the same slice.
- Never change `core.Record`'s or `DiagnosticSink`'s existing signature. `Actor` travels only via `core.WithActor(ctx, ...)` / `core.ActorFrom(ctx)`, never as a domain or application function parameter, and is never persisted, never given a new column or migration, and never exposed on a public DTO.
- Bind the bridge to `catalogo.Service.Create`, never `CreateV2`; composition, not translation, decides which repository backs the service.
- Never reclassify a create-confirm-read error; it passes through the existing `mapError` unchanged.
- `internal/app/*`, `internal/domain/*`, `internal/postgres/*`, `cmd/garfex/`, and `internal/tui/` stay at zero changed lines for the entire chain. `internal/core/errors.go`'s `Map` is unchanged — all fifteen categories and their precedence already exist.
- No unit compiles, exports, or stubs `Update`, `Deactivate`, `Reactivate`, or `HardDelete` as a symbol, an "unimplemented" branch, an unused request field, or any other runtime-discoverable artifact.
- The READ contract stays byte-compatible: no read DTO, method, or error identity changes in any unit.
- Inspect `git diff --name-only` and `git diff --stat` before and after every unit. Preserve unrelated user changes and preserve `openspec/changes/resource-master-technical-debt/` and any other untouched change directory without edits.
- Do not run local `go build`, `docker build`, or blanket database/volume cleanup. This change touches no migration and no PostgreSQL SQL; bridge-layer evidence uses in-repo fakes for `catalogWriter`/`resourceWriter`, not a live database.
- When work cannot fit in 400 changed lines, split into another additive unit along the design's own file/table granularity rather than weakening these rules.

## Ordered implementation units

### W1 — Public write contract

- [x] Add the write request DTOs (`CatalogWriteRequest`, `ResourceWriteRequest`), `WriteCapabilities`, `Writer`, `NewWriter`, and shape validation in the new `resourcecore/write_types.go` and `resourcecore/writer.go`; extend `resourcecore/copy.go` with `CloneCatalogWriteRequest`/`CloneResourceWriteRequest` built on the existing `CloneValue`/`CloneStringSlice` primitives; add coverage in the new `resourcecore/writer_test.go` and extend `resourcecore/external_test.go`. Satisfies spec requirements "Consumer-neutral public Create for catalog and resource," "Actor attribution without persistence" (the `Actor` field and blank-`Actor` gate), and "Compiled surface limited to graduated Create." <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `resourcecore/write_types.go`, `resourcecore/writer.go`, `resourcecore/copy.go`, `resourcecore/writer_test.go`, and `resourcecore/external_test.go`.

**Dependencies and end state:** start from the accepted READ-ONLY `resourcecore` baseline; `Reader`/`ReadCapabilities` remain untouched byte-for-byte. `Writer` validates request shape only, per design.md's fixed list: nil capability; blank `Actor` after `strings.TrimSpace`; unknown `Kind` (reuses `isKnownKind`); nil/empty `Values`; a `Value.Kind` outside the tagged union; a canonical numeric string that fails to parse; a non-zero `Reference.ID`; a non-empty `UnitCode` on a non-`QUANTITY` value or an empty one on a `QUANTITY` value; a blank `Scope.ClassCode`/`FamilyCode`/`TypeCode`/`NaturalUnit`; an `ApplicabilityRule.Equals` whose `Kind` is not `TEXT`; a `Mode` outside `REQUIRED|OPTIONAL|CONDITIONAL|FORBIDDEN`. Everything else (reference existence, catalog validity, uniqueness, identity) is explicitly left to the Core. `WriteCapabilities` compiles exactly `CreateCatalog`/`CreateResource`; no ungraduated method exists as a symbol, stub, or discoverable artifact. Requests and returned records are deep-copied at both edges. Nothing implements `WriteCapabilities` in production yet, so no bridge or composition is affected by this unit. Rollback removes only these five `resourcecore` files.

**Focused checks:** `go test ./resourcecore -run 'TestWriter|TestExternalWrite|TestWriteRequestCopy' -count=1`.

**Strict evidence:** RED adds, before implementation: `TestWriter_NewWriter_NilCapability_InvalidArgument`, `TestWriter_CreateCatalog_ShapeValidation` (unknown kind, tagged-union mismatch, non-zero `Reference.ID`, `UnitCode` rules, blank `Actor`), `TestWriter_CreateResource_ShapeValidation` (blank `Scope` fields, `Mode` enum, `ApplicabilityRule.Equals` kind), `TestWriter_NoUngraduatedMethodExported` (reflection over `WriteCapabilities`/`Writer`), `TestWriteRequestCopy_CallerMutationAfterCall_NoEffect`, and `TestExternalWrite_ConsumerConstructsWriterUsingPublicTypesOnly` (a package-external fake `WriteCapabilities` implementing both methods, proving both compile and succeed with only public types — the spec's "External consumer creates catalog and resource records" scenario). GREEN implements the smallest DTOs/`Writer`/copies that satisfy shape validation only, with no business decision. TRIANGULATE covers every condition in the shape-gate list above plus nil-vs-empty `Values`/`Rules` and `Rules == nil` (omitted aggregate) vs non-nil-empty (explicit rule-free aggregate). REFACTOR keeps `Writer` free of any reference-existence, catalog-validity, uniqueness, or identity decision.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` and record compile/full-suite success before W2.

### W2 — Catalog Create bridge + actor seam

- [x] Add `core.WithActor`, `core.ActorFrom`, `core.NewDiagnosticRecord`, and `DiagnosticRecord.Actor` in `internal/core/diagnostics.go`; widen `catalogPort` with a new `catalogWriter{Create}` seam and add `Adapter.CreateCatalog` plus the inverse catalog value/rule mappers in `internal/bridge/resourcecore/adapter.go`, adapting every in-repo bridge fake in the same slice; add coverage in `internal/core/errors_test.go` and `internal/bridge/resourcecore/adapter_test.go`. Satisfies spec requirements "Write-direction field and query completeness," "Actor attribution without persistence" (the diagnostic seam), and the catalog half of "Create-reachable error category coverage." <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `internal/core/diagnostics.go`, `internal/core/errors_test.go`, `internal/bridge/resourcecore/adapter.go`, and `internal/bridge/resourcecore/adapter_test.go`.

**Dependencies and end state:** W1 is green. `core.Record`'s and `DiagnosticSink`'s existing signatures stay byte-identical; the six existing `core.Record` call sites (`internal/app/catalogo/service.go:375,380,390`, `internal/postgres/catalog_admin_repository.go:227,248`, `internal/postgres/resource_repository_codec.go:115`) are untouched. `catalogPort` = `catalogReader` + new `catalogWriter`; `*catalogo.Service` already satisfies it — only in-repo test fakes gain the method. `Adapter.CreateCatalog` calls `catalogo.Service.Create` (never `CreateV2`), converts `Actor` into `core.WithActor(ctx, req.Actor)` (ctx only, never a business parameter, never persisted), and maps all six `domain.CatalogRecord` fields per the design's field-completeness table: `Kind`→`Create`'s kind argument, `Active`→verbatim, `Values`→per-descriptor-field-kind inverse of `mapCatalogValue` (7 field kinds: `FieldText/FieldCode`→`Text`, `FieldBool`→`Bool`, `FieldInt`→`Int` via `strconv.Atoi` with overflow → `INVALID_ARGUMENT`, `FieldStringList`→`List` cloned, `FieldRef`→`Ref.Kind/Ref.Code`, `FieldEnum`→`Text`), `Rules`→complete-replacement inverse of all six `ApplicabilityRule` fields (`AttributeCode`, `Equals`, `Mode`, `IdentityParticipates`, `NotApplicable`, `Active`); `ID`/`Revision` are intentionally absent from the create request (assigned by persistence). `domain.CatalogRef.Label` receives no public source by design (repository-resolved presentation, not a mutation key) — documented at the omission site. `internal/app/catalogo`, `internal/domain`, `internal/postgres` remain unchanged. Rollback removes only these four files.

**Focused checks:** `go test ./internal/core ./internal/bridge/resourcecore -run 'TestWriteBridge|TestActor|TestCatalogCreate' -count=1`.

**Strict evidence:** RED adds, before implementation: `TestActor_WithActor_ActorFrom_RoundTrip` and `TestActor_NewDiagnosticRecord_IncludesActorAndBlankNoop` in `internal/core/errors_test.go` (also switching the existing hand-built `DiagnosticRecord` at `errors_test.go:98` to the new constructor in this same slice); `TestWriteBridge_CatalogPort_FakeAdapted`, `TestWriteBridge_CatalogCreate_FieldCompleteness` (against `domain.CatalogRecord`'s exact six fields), `TestCatalogCreate_ValueMapping_InverseRoundTrip` (all 7 field kinds), `TestCatalogCreate_RuleMapping_AllSixFieldsMapped`, and `TestCatalogCreate_NineCategoryTable_DistinctAndNoLeakage` (injects each of the 9 Create-reachable sentinel families into the `catalogWriter` fake and asserts exact public code, distinctness, and no leakage of `Error()`/`%v`/`%+v`/concrete type/`errors.Unwrap`/recursive chain of pgx/`PgError`/SQLSTATE/constraint/table/column/server text) in `internal/bridge/resourcecore/adapter_test.go`. GREEN implements the seam, mappers, and diagnostic helpers. TRIANGULATE proves `Active: false` round-trips (open question 1 in design.md — if the domain forces an active create instead, this unit reports `MISSING DOMAIN CRITERION` and drops the field rather than silently ignoring it), non-nil-empty `Rules` vs nil `Rules`, `INTEGER` overflow rejection, and every non-coercible `Value.Kind`/`FieldKind` pairing rejected as `INVALID_ARGUMENT`. REFACTOR confirms no reference-existence, uniqueness, or lifecycle decision entered the bridge.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` and record compile/full-suite success before W3.

### W3 — Resource Create bridge

- [x] Widen `resourcePort` with a new `resourceWriter{Create}` seam and add `Adapter.CreateResource` with the create-confirm read and the inverse attribute mapper in `internal/bridge/resourcecore/adapter.go`, adapting every in-repo bridge fake in the same slice; add field-completeness, inverse round-trip, and 9-category coverage in `internal/bridge/resourcecore/adapter_test.go`, plus the resource half of the external-consumer proof in `resourcecore/external_test.go`. Satisfies spec requirements "Consumer-neutral public Create for catalog and resource" (resource half), "Write-direction field and query completeness" (resource half), "Actor attribution without persistence" (resource half), and the resource half of "Create-reachable error category coverage." <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `internal/bridge/resourcecore/adapter.go`, `internal/bridge/resourcecore/adapter_test.go`, and `resourcecore/external_test.go`.

**Dependencies and end state:** W2 is green. `resourcePort` = `resourceReader` + new `resourceWriter`; `*recursos.Service` already satisfies it — only in-repo test fakes gain the method. `Adapter.CreateResource` calls `recursos.Service.Create`, converts `Actor` into `core.WithActor(ctx, req.Actor)`, and maps all three `domain.CreateCommand` fields: `Scope.ClassCode/FamilyCode/TypeCode`, `NaturalUnit`, and `Attributes` (7 graduated `Value.Kind`s map to `domain.ResourceAttributeValue` — `CONTROLLED_OPTION`, `INTEGER`, `DECIMAL`, `QUANTITY` (`UnitCode` required, decimal parsed via `shopspring/decimal`, never `float64`), `BOOLEAN`, `NOT_APPLICABLE`, `TEXT`; 4 unmappable kinds — `CODE`, `ENUM`, `STRING_LIST`, `REFERENCE` — are rejected `INVALID_ARGUMENT`, never coerced). Because `resourceRepository.Create` returns no identity/revision, `Adapter.CreateResource` performs exactly one non-transactional create-confirm read (`resources.Get(ctx, req.Scope.ClassCode, created.IdentityKey)`) and projects that persisted resource; confirm-read errors pass through the existing `mapError` unchanged, with no reclassification (this is also the concrete, provable source of Create-reachable `NOT_FOUND` for resources). `internal/app/recursos`, `internal/domain`, `internal/postgres` remain unchanged. Rollback removes only these three files.

**Focused checks:** `go test ./internal/bridge/resourcecore ./resourcecore -run 'TestWriteBridge|TestResourceCreate|TestExternalWrite' -count=1`.

**Strict evidence:** RED adds, before implementation: `TestWriteBridge_ResourcePort_FakeAdapted`, `TestResourceCreate_FieldCompleteness_AgainstCreateCommand` (against `domain.CreateCommand`'s exact three fields), `TestResourceCreate_AttributeMapping_InverseRoundTripSevenKinds` (the round-trip obligation `mapResourceAttributeValue(toDomainAttribute(v)) == v` for every graduated kind), `TestResourceCreate_AttributeMapping_RejectsFourUnmappableKinds`, `TestResourceCreate_ConfirmRead_IdentityV1NoReclassification` (asserts `ID > 0`, `Revision >= 1`, durable `identity-v1`, and that a confirm-read `NOT_FOUND` is returned unchanged rather than reclassified), and `TestResourceCreate_NineCategoryTable_DistinctAndNoLeakage` (same injected-sentinel shape as W2, against `resourceWriter`) in `internal/bridge/resourcecore/adapter_test.go`; `TestExternalWrite_ConsumerCreatesResourceUsingPublicTypesOnly` in `resourcecore/external_test.go`. GREEN implements the seam, confirm read, and mapper. TRIANGULATE covers `QUANTITY`'s required `UnitCode` vs the other six kinds' must-be-empty `UnitCode`, decimal parsing with no `float64` conversion anywhere in `resourcecore`, and every unmappable kind rejected without coercion. REFACTOR confirms no reclassification and no business decision entered the bridge.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` and record compile/full-suite success before W4.

### W4 — Create readiness record, documentation, and zero-touch confirmation

- [x] Create the Create readiness record at `openspec/changes/resource-master-core-write/readiness.md`, in the style of the archived READ-ONLY `readiness.md` gate; confirm via a read-only textual-reference scan that `cmd/garfex/` and `internal/tui/` carry zero changed lines and zero new `resourcecore` references; re-run the full verification suite. Satisfies spec requirement "Compiled surface limited to graduated Create" (the zero-changed-lines / zero-reference half) and closes out the readiness documentation success criteria from proposal.md. <!-- sdd-owner: implementation -->

**Allowed edit surfaces:** exactly `openspec/changes/resource-master-core-write/readiness.md`. No `.go` file may be edited in this unit; this unit is documentation plus verification only.

**Dependencies and end state:** W3 is green. This unit is design.md's "W3 — resource Create bridge + readiness" row split along the design's own file/table granularity (`readiness.md` is already a distinct row in design.md's "Files and change surfaces" table), so the resource-bridge implementation (W3) and the readiness/documentation/zero-reference evidence (W4) each stay comfortably inside the 400-line budget instead of risking the "High" budget risk design.md itself flags. Before any edit, run a read-only textual scan — `git diff --stat -- cmd/garfex internal/tui` against the pre-chain baseline, plus `rg resourcecore cmd/garfex internal/tui` — and record zero hits; this is the required evidence for the spec's "Compiled surface limited to graduated Create" scenario. The readiness record states: the verdict; the 9 proven Create-reachable categories (`INVALID_ARGUMENT`, `NOT_FOUND`, `DUPLICATE`, `INVALID_REFERENCE`, `VALIDATION`, `INTEGRITY`, `INVALID_CATALOG`, `UNAVAILABLE`, `INTERNAL`) with evidence pointers into W2/W3; the 6 categories that stay unproven from Create (`CONFLICT`, `IDENTITY_CONFLICT`, `INVALID_LIFECYCLE`, `REACTIVATION_IMPOSSIBLE`, `IN_USE`, `IMMUTABLE_CODE`); the resolution of design.md's two open questions (`CatalogWriteRequest.Active` round-trip; catalog-side `NOT_FOUND` reachability from Create); the known diagnostic-attribution gap (three `internal/postgres` sites that still call `core.Record` with `context.Background()`, recorded as an INFO-level follow-up, not silently ignored); the create-confirm read's non-transactional, one-writer-topology assumption; and the explicit list of unavailable operations (`Update`, `Deactivate`, `Reactivate`, `HardDelete`) with a pointer to design.md's "complete eventual shape" table. Rollback removes only the readiness record.

**Focused checks:** full-suite only — `go test ./... -count=1`, `gofmt -l .`, `go vet ./...`, `golangci-lint run ./...`. No new `.go` test names are added in this unit, so no `-run` pattern applies; this is consistent with the archived precedent's documentation-only unit (6C), which used the same full-suite-plus-lint shape.

**Strict evidence:** RED reviews the readiness draft against `proposal.md`, `design.md`, and `specs/resource-master-core/spec.md`'s ten scenarios and two open questions, and against the zero-reference scan output, before it is accepted. GREEN records only the confirmed evidence — no aspirational or unverified claim. TRIANGULATE independently re-runs the zero-reference scan and cross-checks the 9-category and field-completeness evidence produced in W2/W3 against the readiness record's claims. REFACTOR simplifies the record for reviewer scanning without weakening any claim.

**Mandatory slice boundary:** after the focused command, run `go test ./... -count=1` one final time and record compile/full-suite success before the parent-controlled gate.

## Parent-controlled gate after the final unit

- [x] G1 — Create-WRITE readiness. After W4, recalculate Create-WRITE readiness from `resourcecore/`, `internal/bridge/resourcecore/`, `internal/core/diagnostics.go`, and `openspec/changes/resource-master-core-write/readiness.md`; confirm all of the following before treating Create-WRITE as ready:
  - (a) an external Go package constructs a `Writer` and calls both `CreateCatalog` and `CreateResource` successfully with no `internal` import (spec: "Consumer-neutral public Create for catalog and resource");
  - (b) every public write-request field is mapped at the bridge or carries a one-line omission rationale, for both catalog (`domain.CatalogRecord`) and resource (`domain.CreateCommand`) (spec: "Write-direction field and query completeness");
  - (c) all 9 Create-reachable categories are proven reachable and distinct for both catalog and resource, and `CONFLICT`/`IDENTITY_CONFLICT`/`INVALID_LIFECYCLE`/`REACTIVATION_IMPOSSIBLE`/`IN_USE`/`IMMUTABLE_CODE` are named as unreachable, not silently absent (spec: "Create-reachable error category coverage");
  - (d) `Actor` reaches the internal diagnostic seam via `core.WithActor`/`core.ActorFrom` only, is never persisted, introduces no new column or migration, and appears on no public DTO (spec: "Actor attribution without persistence");
  - (e) write requests and returned records are defensively copied at both edges; a caller mutating either after the call changes nothing in flight or persisted (spec: "Consumer-neutral public Create for catalog and resource," scenario "Caller mutation after the call has no effect");
  - (f) `cmd/garfex/` and `internal/tui/` show zero changed lines and zero `resourcecore` references, confirmed by the W4 textual-reference scan (spec: "Compiled surface limited to graduated Create");
  - (g) only `CreateCatalog`/`CreateResource` exist on `Writer`/`WriteCapabilities`, with no ungraduated stub for `Update`/`Deactivate`/`Reactivate`/`HardDelete`;
  - (h) `go test ./... -count=1`, `gofmt -l .`, `go vet ./...`, and `golangci-lint run ./...` all pass; CI-only race/build results are reported separately, as receipts, not as a gate substitute.

  Do not apply or plan `Update`, `Deactivate`, `Reactivate`, or `HardDelete` in this change; each remains a separate, later, per-operation-gated change with its own readiness evidence, per the archived spec's "Failing evidence withdraws only affected write readiness" and this change's own non-goals. <!-- sdd-owner: parent -->
  - Confirmed 2026-08-21: recorded in full in `openspec/changes/resource-master-core-write/readiness.md`. Verdict: **Create-WRITE is ready**, with one precisely scoped, honestly documented gap — catalog Create's `NOT_FOUND` category is not end-to-end reachable (no lookup-by-ID exists anywhere in `insertLocked`'s path; resource Create's `NOT_FOUND` remains genuinely reachable via its create-confirm read). All other 8 categories proven reachable and distinct for both domains with no leakage. Zero-touch confirmation for `cmd/garfex/`/`internal/tui/` verified directly (`rg`/`git diff --stat` both empty), not assumed. `golangci-lint`/`go vet`/`gofmt` clean across every unit; `go test ./... -count=1` green for every package this change touches, with only the known pre-existing, untracked, unrelated `agent/skills/golang-cli/assets/examples` failure persisting (unrelated since 6A of `stabilize-resource-master-core`).

## Final graph self-audit

- Unit order is exactly public write contract (W1) → catalog Create bridge + actor seam (W2) → resource Create bridge (W3) → readiness/documentation/zero-touch confirmation (W4), matching design.md's slice-transition table with W3 split along its own file/table granularity.
- No unit edits `internal/app/*`, `internal/domain/*`, `internal/postgres/*`, `cmd/garfex/`, or `internal/tui/`; W4 is the explicit, textual-reference-scanned proof for the driving-adapter half of that invariant.
- `internal/core/errors.go`'s `Map` is never edited; all fifteen categories and their precedence already exist and are reused as-is.
- Every port widening (`catalogPort` in W2, `resourcePort` in W3) adapts every in-repo fake in the same slice; no port is edited without every current implementation and test double adapting together.
- `core.Record`'s and `DiagnosticSink`'s signatures never change across the chain; only additive helpers (`WithActor`, `ActorFrom`, `NewDiagnosticRecord`, `DiagnosticRecord.Actor`) are added, in W2.
- Every unit has a concrete start dependency, exact edit surfaces, focused RED/GREEN/TRIANGULATE/REFACTOR evidence (or, for W4, review/re-verification evidence), a rollback boundary, and mandatory `go test ./... -count=1` compile/full-suite proof.
- The graph contains no `Update`/`Deactivate`/`Reactivate`/`HardDelete` implementation tasks; each remains future work after its own later, separate per-operation readiness decision, exactly as proposal.md's non-goals require.
- Only `openspec/changes/resource-master-core-write/tasks.md`, the four `resourcecore`/`internal/core`/`internal/bridge/resourcecore` source-and-test surfaces named above, `openspec/changes/resource-master-core-write/readiness.md`, and the corresponding Engram topic are affected by this plan; the archived `stabilize-resource-master-core` artifacts and any unrelated in-flight change remain untouched.
